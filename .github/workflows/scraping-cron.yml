name: Scraping Automático (DESABILITADO - Usando VPS)

on:
  # DESABILITADO: Scraping agora é executado na VPS
  # schedule:
  #   - cron: '0 */12 * * *'
  workflow_dispatch: # Permite execução manual via interface do GitHub (apenas para testes)

jobs:
  # DESABILITADO: Scraping agora é executado na VPS
  # VPS: Hostinger (31.97.174.90)
  # Frequência: A cada 6 horas (00:00, 06:00, 12:00, 18:00)
  scrape-disabled:
    runs-on: ubuntu-22.04
    timeout-minutes: 1
    
    steps:
      - name: ⚠️ WORKFLOW DESABILITADO
        run: |
          echo "🚫 Este workflow foi desabilitado!"
          echo "📡 O scraping agora é executado na VPS a cada 6 horas"
          echo "🖥️  VPS: Hostinger (31.97.174.90)"
          echo "⏰ Frequência: A cada 6 horas (00:00, 06:00, 12:00, 18:00)"
          echo "📊 Para executar manualmente, use: pm2 restart scraping-cron na VPS"
          echo "✅ Status: VPS funcionando perfeitamente!"
          echo ""
          echo "🔧 Comandos úteis na VPS:"
          echo "   - Ver status: pm2 status"
          echo "   - Ver logs: pm2 logs scraping-cron"
          echo "   - Reiniciar: pm2 restart scraping-cron"
          echo "   - Executar manual: ./run-scraping.sh"